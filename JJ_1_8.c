#include <stdio.h>

int main()

{
    float a, b;

    printf("Enter the number of Dollars");
    scanf("%f", &a);

    b = a * 48;

    printf("The number of Rs. is: %f", b);

    return 0;
}