#include <stdio.h>
int main()

{
    float R,A;

    printf("Enter radius : ");
    scanf("%f",&R);

    A = (3.14) *R *R;

    printf("The area is the circle is %f: ",A);

    return 0;
}